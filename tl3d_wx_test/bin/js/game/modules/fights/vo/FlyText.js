var game;
(function (game) {
    var FlyText = /** @class */ (function () {
        function FlyText() {
        }
        return FlyText;
    }());
    game.FlyText = FlyText;
})(game || (game = {}));
